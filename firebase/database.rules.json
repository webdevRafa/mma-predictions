{
  "rules": {
    ".read": false,
    ".write": false,
    "chat": {
      "v1": {
        "rooms": {
          "$roomId": {
            "messages": {
              ".read": true,
              ".write": false,
              ".indexOn": ["createdAt"]
            },
            "presence": {
              ".read": true,
              "$uid": {
                ".write": "$uid === auth.uid",
                ".validate": "newData.val() === null || (newData.numChildren() === 2 && newData.hasChildren(['connected', 'lastSeen']) && newData.child('connected').isBoolean() && newData.child('lastSeen').isNumber())"
              }
            },
            "typing": { ".read": false, ".write": false }
          }
        },
        "rateLimits": { ".read": false, ".write": false }
      }
    }
  }
}
