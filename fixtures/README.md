# Normalized fixtures

Fixtures are versioned development inputs for the `MockMmaProvider`, emulator seeds, and contract tests. They are UFC-only but use fictional athletes and events. They are not a licensed production feed and must never be presented as official live data.

Run `pnpm validate:fixtures` after every fixture change. Validation failures include a JSON-style path and reason.
